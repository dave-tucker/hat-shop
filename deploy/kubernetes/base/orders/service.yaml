apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: hat-shop
  labels:
    app: orders
spec:
  selector:
    app: orders
  ports:
    - port: 8080
      targetPort: 8080
      name: http
