apiVersion: v1
kind: Service
metadata:
  name: payments
  namespace: hat-shop
  labels:
    app: payments
spec:
  selector:
    app: payments
  ports:
    - port: 8080
      targetPort: 8080
      name: http
