apiVersion: v1
kind: Service
metadata:
  name: shipping
  namespace: hat-shop
  labels:
    app: shipping
spec:
  selector:
    app: shipping
  ports:
    - port: 8080
      targetPort: 8080
      name: http
